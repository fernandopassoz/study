IO.puts("Elixir")

IO.puts(1 + 1)

IO.puts(round(3.7))

IO.puts(:allo)

IO.puts(true == true)

IO.puts(is_atom(false))

IO.puts(is_atom("hello"))

string = :allo

IO.puts("Olá #{string}")

IO.puts(is_binary(string))

IO.puts(is_binary("string"))

IO.puts(byte_size("string"))
IO.puts(byte_size("string"))

String.length("hello")
